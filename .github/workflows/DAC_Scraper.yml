name: Fetch NMLS Data

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      # --------------------------------------------------
      # Checkout Repo
      # --------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # --------------------------------------------------
      # Setup Node.js
      # --------------------------------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # --------------------------------------------------
      # Install Chrome + Xvfb + Required Libraries
      # --------------------------------------------------
      - name: Install Chrome, Xvfb and required libraries
        run: |
          set -euxo pipefail
          sudo apt-get update

          # Core libs
          sudo apt-get install -y \
            xvfb wget ca-certificates \
            fonts-liberation fonts-noto-color-emoji \
            libatk-bridge2.0-0 libatk1.0-0 libcups2 libdbus-1-3 libdrm2 \
            libgbm1 libgtk-3-0 libnspr4 libnss3 libx11-xcb1 libxcomposite1 \
            libxdamage1 libxfixes3 libxrandr2 libxkbcommon0 libwayland-client0 \
            libxshmfence1 libglib2.0-0 xdg-utils libu2f-udev libvulkan1

          # Handle libasound2 name difference across Ubuntu releases
          (sudo apt-get install -y libasound2) || (sudo apt-get install -y libasound2t64)

          # Install Chrome Stable
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt-get install -y ./google-chrome-stable_current_amd64.deb || \
            (sudo apt-get -f install -y && sudo apt-get install -y ./google-chrome-stable_current_amd64.deb)

          google-chrome --version

      # --------------------------------------------------
      # Install OCR dependencies (Tesseract, ImageMagick, Poppler)
      # --------------------------------------------------
      - name: Install OCR dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr imagemagick poppler-utils ghostscript

      # --------------------------------------------------
      # Install Node Dependencies
      # --------------------------------------------------
      - name: Install Node dependencies
        env:
          PUPPETEER_SKIP_DOWNLOAD: 'true'
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi
          npm install googleapis node-fetch puppeteer cheerio pdfkit he

      # --------------------------------------------------
      # Write RAW Service Account JSON
      # --------------------------------------------------
      - name: Write Service Account JSON
        run: |
          echo '${{ secrets.SERVICE_ACCOUNT_JSON }}' > service-account.json

      # --------------------------------------------------
      # Validate service account JSON
      # --------------------------------------------------
      - name: Validate service account JSON
        run: |
          node -e "
            const fs = require('fs');
            const j = JSON.parse(fs.readFileSync('service-account.json','utf8'));
            if (!j.private_key || !j.client_email) throw new Error('Invalid service account JSON');
            console.log('Service account OK:', j.client_email);
          "

      # --------------------------------------------------
      # Run NMLS Scraper
      # --------------------------------------------------
      - name: Run NMLS Scraper via Xvfb
        env:
          PUPPETEER_EXECUTABLE_PATH: /usr/bin/google-chrome
          ZIP_CODE: '33122'
          CAPTCHA_TIMEOUT_MS: '300000'
        run: |
          set -euxo pipefail
          mkdir -p artifacts

          timeout 180s xvfb-run -a --server-args="-screen 0 1920x1080x24" \
            node nmls_elements.cjs || true

      # --------------------------------------------------
      # Run OCR on generated textCaption JPEG
      # --------------------------------------------------
      - name: OCR textCaption image
        if: always()
        run: |
          set -euxo pipefail

          CAPTCHA_IMG="artifacts/textCaption.jpeg"

          if [ ! -f "$CAPTCHA_IMG" ]; then
            echo "❌ No textCaption.jpeg found. OCR skipped."
            exit 0
          fi

          echo "🧠 Running OCR on: $CAPTCHA_IMG"
          node ocr_tesseract.js "$CAPTCHA_IMG"

      # --------------------------------------------------
      # Upload ONLY textCaption JPEG + OCR output
      # --------------------------------------------------
      - name: Upload OCR Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ocr-results
          path: |
            artifacts/textCaption.jpeg
            ocr_output/**
          if-no-files-found: warn

      # --------------------------------------------------
      # Cleanup credentials
      # --------------------------------------------------
      - name: Cleanup secrets
        if: always()
        run: rm -f service-account.json