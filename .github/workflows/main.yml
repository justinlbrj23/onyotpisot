name: Run Python Script with Playwright

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.12'  # Use the stable Python version 3.12

      - name: Write Google credentials to file
        run: |
          echo "${{ secrets.GOOGLE_CREDENTIALS_JSON }}" > credentials.json

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel --disable-pip-version-check --no-cache-dir
          pip install -r requirements.txt --no-cache-dir
        shell: powershell

      - name: Install Playwright Browsers
        run: python -m playwright install
        shell: powershell

      # Optional: If you need to download the auth_state artifact from a previous workflow run
      - name: Download auth_state.json (if necessary)
        uses: actions/download-artifact@v3
        with:
          name: auth_state
          path: .  # Downloads it to the current directory

      # If you're planning to persist the auth_state.json for later use:
      - name: Upload auth_state.json (if necessary)
        uses: actions/upload-artifact@v3
        with:
          name: auth_state
          path: auth_state.json

      # Run your script
      - name: Run main.py
        run: python main.py
        env:
          PYTHONIOENCODING: utf-8
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          AUTH_STATE_PATH: ./auth_state.json  # Ensure the auth_state.json file is used in your script
